# schedule

# SIGIL heartbeat schedule (phase-2 daemon loop deferred; heartbeat_once()
# executes due jobs on demand). Lines: <cron> | <note-or-action>
# Example:
# 0 9 * * * | daily-review
# This file is a placeholder; the runtime loop is not in v1.
